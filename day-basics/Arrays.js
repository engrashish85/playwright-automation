"use strict";
const names = ["Ashish", "Askanksha", "Peter", "Psuhkar"];
console.log(names.length);
console.log(names[0]);
let empNames = ["tom", "oat", "caramal"];
let empId = [1, 3, 2, 2];
let mix = ["tom", 1, "2", 4, "Asihs"];
console.log(`Length is ${mix.length}`);
let values = ["Rom", 1, 2];
for (let i = 0; i < values.length; i++) {
    console.log(values[i]);
}
